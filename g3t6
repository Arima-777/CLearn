#include <stdio.h>
int main(void)
{
	double Mh2o=3e-23;
	int quarta=950;
	float V;
	printf("Введите объём воды в квартах: ");
	scanf("%f", &V);
	printf("Количество молекул воды в указанном объёме: %g шт. (Сокращённая запись с фиксированной запятой)\n", V*quarta/Mh2o);
	//printf("Вы прожили не менее: %.2f секунд (Запись с фиксированной запятой и указанием количества символов после запятой)\n", age*SecInYear);
	return 0;
}